import React, { useContext } from 'react';
import { PageContainer, RouteContext } from '@ant-design/pro-layout';

const SystemManage = () => {
  const context = useContext(RouteContext);
  return <div className="system-manage"></div>;
};
export default SystemManage;
